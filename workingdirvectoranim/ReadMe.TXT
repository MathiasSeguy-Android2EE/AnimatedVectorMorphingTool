Here is the working dir where I donwload SVG, convert them in VectorDrawable and run the tool
To use the last generated jar 'animatedvectortool-0.1.jar',
just copy it from animatedvectortool\build\lib (it's where it's generated by default)